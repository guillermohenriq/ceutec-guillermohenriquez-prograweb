# Nixpacks detecta Python automaticamente por requirements.txt y crea su
# propio entorno virtual con el PATH ya configurado. Solo definimos el
# comando de arranque.
[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --log-file -"
